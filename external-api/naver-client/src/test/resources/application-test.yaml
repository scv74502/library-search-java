spring:
  config:
    test:
      on-profile: local
external:
  naver:
    url: https://openapi.naver.com
    headers:
#      client-id: ${NAVER_API_CLIENT_ID}
#      cliient-secret: ${NAVER_API_CLIENT_SECRET}

